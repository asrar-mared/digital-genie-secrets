# 🧞‍♂️ Digital Genie Secrets - Environment Variables
# انسخ هذا الملف إلى .env وقم بتعديل القيم

# ============================================
# 🔐 Core Security Settings
# ============================================
# مفتاح سري للتطبيق (استخدم: openssl rand -hex 32)
SECRET_KEY=your-super-secret-key-change-this-in-production

# مفتاح التشفير (استخدم: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-change-this-now

# مفتاح JWT (استخدم: openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-key-change-this-immediately

# مدة صلاحية الـ Token (بالدقائق)
JWT_EXPIRATION_MINUTES=60

# مدة صلاحية Refresh Token (بالأيام)
JWT_REFRESH_EXPIRATION_DAYS=30

# ============================================
# 🌐 Application Settings
# ============================================
# البيئة: development, staging, production
ENV=development

# وضع Debug (false في الإنتاج!)
DEBUG=true

# اسم التطبيق
APP_NAME=Digital Genie Secrets

# الإصدار
VERSION=3.0.1

# المضيف والمنفذ
HOST=0.0.0.0
PORT=8443
HTTP_PORT=8080

# عنوان URL الأساسي
BASE_URL=https://digital-genie.local

# المجال
DOMAIN=digital-genie.local

# ============================================
# 🗄️ Database Configuration
# ============================================
# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=digital_genie
DB_USER=genie
DB_PASSWORD=change-this-strong-password-now

# Connection Pool
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# URL كامل للقاعدة
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ============================================
# 🔴 Redis Configuration
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change-redis-password-now
REDIS_DB=0

# URL كامل لـ Redis
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# Cache TTL (بالثواني)
CACHE_TTL=3600

# ============================================
# 🍃 MongoDB Configuration
# ============================================
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=genie_logs
MONGO_USER=admin
MONGO_PASSWORD=change-mongo-password-now

# URL كامل لـ MongoDB
MONGODB_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DB}

# ============================================
# 🔄 Celery Configuration
# ============================================
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}
CELERY_WORKER_CONCURRENCY=4
CELERY_TASK_TIME_LIMIT=3600
CELERY_TASK_SOFT_TIME_LIMIT=3000

# ============================================
# 📧 Email Configuration
# ============================================
# SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@digital-genie.com
SMTP_TLS=true

# SendGrid (بديل)
SENDGRID_API_KEY=your-sendgrid-api-key

# ============================================
# 📱 SMS/Notifications
# ============================================
# Twilio
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Slack
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# ============================================
# 🔐 SSL/TLS Configuration
# ============================================
SSL_CERT_PATH=/app/ssl/cert.pem
SSL_KEY_PATH=/app/ssl/key.pem
SSL_CA_PATH=/app/ssl/ca.pem

# Generate self-signed cert:
# openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#   -keyout ssl/key.pem -out ssl/cert.pem

# ============================================
# 🛡️ Security Features
# ============================================
# تفعيل 2FA
ENABLE_2FA=true

# الحد الأقصى لمحاولات تسجيل الدخول
MAX_LOGIN_ATTEMPTS=5

# مدة قفل الحساب (بالدقائق)
ACCOUNT_LOCKOUT_DURATION=30

# تفعيل Rate Limiting
ENABLE_RATE_LIMIT=true

# الحد الأقصى للطلبات في الدقيقة
RATE_LIMIT_PER_MINUTE=60

# تفعيل CORS
ENABLE_CORS=true

# النطاقات المسموح بها
CORS_ORIGINS=http://localhost:3000,https://digital-genie.com

# ============================================
# 🔍 Threat Detection
# ============================================
# تفعيل IDS/IPS
ENABLE_IDS=true

# مستوى الحساسية: low, medium, high, critical
IDS_SENSITIVITY=high

# تفعيل AI Detection
ENABLE_AI_DETECTION=true

# ملف نموذج الـ AI
AI_MODEL_PATH=/app/models/threat_detection.h5

# ============================================
# 📊 Monitoring & Logging
# ============================================
# مستوى السجل: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# تنسيق السجل: json, text
LOG_FORMAT=json

# مسار السجلات
LOG_PATH=/app/logs

# حجم ملف السجل الأقصى (MB)
LOG_MAX_SIZE=100

# عدد ملفات السجل الاحتياطية
LOG_BACKUP_COUNT=10

# Sentry DSN (تتبع الأخطاء)
SENTRY_DSN=your-sentry-dsn-url

# ============================================
# 📈 Metrics & Analytics
# ============================================
# تفعيل Prometheus
ENABLE_PROMETHEUS=true

# منفذ Metrics
METRICS_PORT=9090

# تفعيل Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=change-grafana-password

# ============================================
# 🌸 Flower (Celery Monitoring)
# ============================================
FLOWER_USER=admin
FLOWER_PASSWORD=change-flower-password
FLOWER_PORT=5555

# ============================================
# 🔍 Elasticsearch & Kibana
# ============================================
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_USER=elastic
ELASTICSEARCH_PASSWORD=change-elastic-password

KIBANA_HOST=kibana
KIBANA_PORT=5601

# ============================================
# ☁️ Cloud Storage (اختياري)
# ============================================
# AWS S3
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=digital-genie-backups

# Google Cloud Storage
GCS_PROJECT_ID=your-project-id
GCS_BUCKET=digital-genie-backups
GCS_CREDENTIALS_PATH=/app/config/gcs-credentials.json

# Azure Blob Storage
AZURE_STORAGE_ACCOUNT=yourstorageaccount
AZURE_STORAGE_KEY=your-storage-key
AZURE_CONTAINER=digital-genie-backups

# ============================================
# 🔗 External APIs
# ============================================
# VirusTotal API
VIRUSTOTAL_API_KEY=your-virustotal-api-key

# AlienVault OTX
ALIENVAULT_API_KEY=your-alienvault-api-key

# Shodan
SHODAN_API_KEY=your-shodan-api-key

# Have I Been Pwned
HIBP_API_KEY=your-hibp-api-key

# ============================================
# 🧪 Testing
# ============================================
# قاعدة بيانات الاختبار
TEST_DATABASE_URL=postgresql://genie:test@localhost:5432/genie_test

# تفعيل Coverage
ENABLE_COVERAGE=true

# ============================================
# 🚀 Performance
# ============================================
# عدد Workers
WORKERS=4

# عدد Threads لكل Worker
WORKER_THREADS=2

# Timeout (بالثواني)
WORKER_TIMEOUT=120

# Keep Alive (بالثواني)
KEEP_ALIVE=5

# Max Requests per Worker
MAX_REQUESTS=1000

# Max Requests Jitter
MAX_REQUESTS_JITTER=100

# ============================================
# 💾 Backup Configuration
# ============================================
# تفعيل النسخ الاحتياطي التلقائي
ENABLE_AUTO_BACKUP=true

# توقيت النسخ الاحتياطي (cron format)
BACKUP_SCHEDULE=0 2 * * *

# مدة الاحتفاظ بالنسخ (بالأيام)
BACKUP_RETENTION_DAYS=30

# مسار النسخ الاحتياطية
BACKUP_PATH=/app/backups

# ============================================
# 🌍 Internationalization
# ============================================
# اللغة الافتراضية
DEFAULT_LANGUAGE=ar

# اللغات المدعومة
SUPPORTED_LANGUAGES=ar,en,fr,es

# المنطقة الزمنية
TIMEZONE=Africa/Cairo

# ============================================
# 🎨 Frontend (إذا كان موجوداً)
# ============================================
FRONTEND_URL=http://localhost:3000
FRONTEND_BUILD_PATH=/app/frontend/build

# ============================================
# 📝 ملاحظات مهمة
# ============================================
# 1. غيّر جميع كلمات المرور والمفاتيح السرية!
# 2. لا ترفع هذا الملف على GitHub!
# 3. استخدم مدير أسرار (Vault, AWS Secrets Manager) في الإنتاج
# 4. قم بتفعيل HTTPS في الإنتاج
# 5. راجع إعدادات الأمان بانتظام

# ============================================
# 🔧 توليد المفاتيح السرية
# ============================================
# openssl rand -hex 32  # لتوليد مفاتيح عشوائية
# python -c "import secrets; print(secrets.token_hex(32))"
